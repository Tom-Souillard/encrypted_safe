name: Continuous Integration

# This workflow is triggered on pushes and pull requests to the main branch.
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - uses: actions/checkout@v2
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it

    - name: Set up Python 3.12
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'
        # Set up a specific version of Python

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      # Install the Python dependencies defined in requirements.txt

    - name: Run tests
      run: |
        echo "Running tests..."
      # This is a placeholder for running your actual test suite, e.g., pytest

    - name: Build Docker image
      run: |
        docker build . -t encrypted_safe:latest
      # Build the Docker image using the Dockerfile in your repository

    - name: Push Docker image
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: |
        echo "Docker Push Placeholder"
      # This placeholder should be replaced with actual Docker push commands
